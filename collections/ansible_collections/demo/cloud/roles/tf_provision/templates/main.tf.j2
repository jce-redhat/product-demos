terraform {
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "5.0"
    }
  }

  backend "s3" {
    bucket = "{{ tf_provision_s3_bucket }}"
    region = "{{ tf_provision_aws_region }}"
    key    = "product-demos.tfstate"
  }
}

provider "aws" {
  region  = "{{ tf_provision_aws_region }}"
}
